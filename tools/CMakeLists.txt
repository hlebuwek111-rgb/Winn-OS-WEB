add_custom_target(mkbootfs
  COMMENT "Placeholder target for boot filesystem image builder")
add_custom_target(mkfs_wfs
  COMMENT "Placeholder target for WFS formatting utility")
add_custom_target(build_iso
  COMMENT "Placeholder target for ISO image assembly script")
